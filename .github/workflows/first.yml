name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout
          uses: actions/checkout@v4
        
        - name: Welcome
          run: echo "Hello World"

        - name: List files
          run: ls

        - name: Read files
          run: cat README.md